<%= form_for @micropost do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t(:post_form_plh) %>
  </div>
  <div class="actions">
    <%= f.submit t(:post_create_btn) %>
  </div>
<% end %>